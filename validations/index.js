const validateAge = require('./validateAge');
const validateName = require('./validateName');
const validateRate = require('./validateRate');
const validateWhatchedAt = require('./validateWatchedAt');

module.exports = { validateName, validateAge, validateWhatchedAt, validateRate };
